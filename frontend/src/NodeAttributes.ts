export default class NodeAttributes {
  position: string
  evaluation: number
  constructor(position: string, evaluation: number) {
    this.position = position
    this.evaluation = evaluation
  }
}
